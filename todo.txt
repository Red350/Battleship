Change colour of Xs over ship when ship is destroyed.

Store the current colour of the X so it doesn't have to check each time it renders.

Make hard and medium ai inherit correctly.

Add menu.

Have ai parse game board after sinking ship, checking for partially sunk ships.

Ships picked up after randomise can not be placed on the squares they were just on